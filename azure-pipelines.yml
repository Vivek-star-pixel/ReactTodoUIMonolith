trigger: none 

pool: Default

stages:
- stage: setup_nodejs
  displayName: install_runtime_env_node
  
  jobs:
  - job: installnodejs
    steps:

    -  checkout: self
    - task: NodeTaskRunnerInstaller@0
      inputs:
        nodeVersion: '16'

